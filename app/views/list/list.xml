<Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:picker="xml-declaration/picker/Picker" loaded="main">
    <Page.actionBar>
        <ActionBar title="Манчкины">
            <ActionBar.actionItems>
                <ActionItem text="Добавить" ios.position="right" tap="onCreate" />
            </ActionBar.actionItems>
        </ActionBar>
    </Page.actionBar>
    <StackLayout>
        <ListView items="{{ players }}" separatorColor="#fff" class="list">
            <ListView.itemTemplate>
                <GridLayout rows="auto" columns="40,*,*" class="item">
                    <Label col="0" text="{{ attack }}" class="attack" tap="onAttack" />
                    <Label col="1" text="{{ name }}" class="name" />
                    <Label col="2" text="{{ level }}" class="level" />
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
        <picker:Picker items="{{ attackPickerItems }}" id="attack-picker" />
    </StackLayout>
</Page>
