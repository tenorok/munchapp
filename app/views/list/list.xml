<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="main" actionBarHidden="true">
    <StackLayout>
        <Button text="Добавить" tap="{{ onCreate }}" class="create" />
        <ListView items="{{ playerList }}">
            <ListView.itemTemplate>
                <Label text="{{ name }}" horizontalAlignment="left" verticalAlignment="center"/>
            </ListView.itemTemplate>
        </ListView>
    </StackLayout>
</Page>
